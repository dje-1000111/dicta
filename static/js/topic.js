var videoTitle = document.getElementById("video-title"), histoCount = [], timeouts = []; if (null == iTag) var iTag = document.querySelector("#ivideo-title"); function create_level_valid() { iTag.classList.add("fa-solid", "fa-check", "text-success") } function del_level_valid() { iTag.classList.remove("fa-solid", "fa-check", "text-success") } function getTip(e) { var t = JSON.stringify(e); t = JSON.parse(t); let s = Object.keys(t).reduce(function (e, s) { return e[t[s]] = s, e }, {}); return s } lines.includes(lineNb) ? create_level_valid() : del_level_valid(), histoCount.push(lineNb), tip = getTip(tips); var timeout = setTimeout(stopVideo, timeouts[lineNb]); for (let i = 0; i < ts.length - 1; i++)timeouts.push(Number((result = (ts[i + 1] - ts[i]) * 1e3 - 200).toFixed(2))); let redleds = document.querySelectorAll(".red"); var redId = 0; if (uia) { var e = screen.width, t = document.getElementById("btn-board"), s = document.getElementById("board"); t.addEventListener("click", function () { "visible" === s.style.visibility ? (t.innerHTML = "Show progress", s.style.transform = "rotateX(90deg) rotateY(0deg)", s.style.transition = "transform 0.5s", setTimeout(function () { s.style.visibility = "hidden" }, 500)) : (t.innerHTML = "Hide progress", s.style.transform = "rotateX(0deg) rotateY(0deg)", s.style.transition = "transform 0.5s", s.style.visibility = "visible") }), e < 540 && t.classList.add("d-none"), window.addEventListener("resize", e => { window.innerWidth < 540 ? (t.classList.add("d-none"), s.classList.add("d-none")) : (t.classList.remove("d-none"), s.classList.remove("d-none")) }) }
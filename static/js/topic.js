var videoTitle = document.getElementById("video-title"), histoCount = [], timeouts = []; if (null == iTag) var iTag = document.querySelector("#ivideo-title"); function create_level_valid() { iTag.classList.add("fa-solid", "fa-check", "text-success") } function del_level_valid() { iTag.classList.remove("fa-solid", "fa-check", "text-success") } function getTip(e) { var t = JSON.stringify(e); t = JSON.parse(t); let l = Object.keys(t).reduce(function (e, l) { return e[t[l]] = l, e }, {}); return l } lines.includes(lineNb) ? create_level_valid() : del_level_valid(), histoCount.push(lineNb), tip = getTip(tips); var timeout = setTimeout(stopVideo, timeouts[lineNb]); for (let i = 0; i < ts.length - 1; i++)timeouts.push(Number((result = (ts[i + 1] - ts[i]) * 1e3 - 200).toFixed(2)));